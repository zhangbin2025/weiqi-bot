# App Configuration
# 编译时注入到 BuildConfig，可通过 local.properties 覆盖

# 本地服务器配置
APP_LOCAL_HOST=localhost
APP_LOCAL_PORT=8088

# 远程服务器配置
APP_REMOTE_BASE=https://bot.weiqi.lol
APP_VERSION_ENDPOINT=/version.json
