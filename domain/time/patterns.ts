/**
 * 时间表达式识别模式配置
 * 
 * 包含正则表达式模式和关键词映射
 */

import type { Frequency } from './types';

/**
 * 周期性任务正则表达式模式
 * 
 * 按优先级排序，优先匹配更具体的模式
 */
export const PERIODIC_REGEX_PATTERNS: RegExp[] = [
  // 每周几 (每周一、每周二...)
  /每周[一二三四五六七日天]/,
  
  // 每月几号 (每月1号、每月15号...)
  /每月\d{1,2}号?/,
  
  // 每 N 天/周/月 (每3天、每2周...)
  /每\d+[天日周月]/,
  
  // 基础周期 (每天、每日、每周、每月)
  /每[天日周月]/,
  
  // 定时、定期
  /定时|定期/,
  
  // 工作日、周末
  /工作日|周末/,
  
  // 隔天、隔日
  /隔[天日]/,
];

/**
 * 关键词到频率的映射
 */
export const PERIODIC_KEYWORDS: Record<string, Frequency> = {
  // 每天相关
  '每天': 'daily',
  '每日': 'daily',
  
  // 每周相关
  '每周': 'weekly',
  '每星期': 'weekly',
  
  // 每月相关
  '每月': 'monthly',
  
  // 工作日/周末
  '工作日': 'workday',
  '周末': 'weekend',
  
  // 定时/定期
  '定时': 'daily',
  '定期': 'daily',
};

/**
 * 星期映射 (中文 -> 数字)
 */
export const WEEK_DAY_MAP: Record<string, number> = {
  '一': 1, '二': 2, '三': 3, '四': 4, '五': 5, '六': 6, '日': 7, '天': 7,
  '周一': 1, '周二': 2, '周三': 3, '周四': 4, '周五': 5, '周六': 6, '周日': 7, '周天': 7,
  '星期一': 1, '星期二': 2, '星期三': 3, '星期四': 4, '星期五': 5, '星期六': 6, '星期日': 7, '星期天': 7,
};
